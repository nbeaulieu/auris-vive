"""
Auris Vive — src package root

Sub-packages
------------
    src.pipeline   Audio analysis pipeline (ingest → separate → transcribe → outputs)
    src.adapters   Input adapters (file, URL, stream, device)
    src.api        FastAPI application + job queue
    src.clients    Web JS SDK and mobile stubs
"""
