"""
src.clients.mobile — iOS and Android SDK stubs

Native SDKs are out of scope for the initial build.  Placeholder only.

iOS:   Swift package wrapping the REST + WebSocket API.
       Likely AVFoundation for device capture → FileAdapter equivalent.

Android: Kotlin, same surface, MediaRecorder for device capture.

Both will share the same API contract as the web SDK.  No Python here —
this __init__.py exists only to complete the monorepo structure.
"""
